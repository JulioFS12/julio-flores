import React from 'react';
import { rocket } from '../assets/images';

export const Space = () => {
    return (
        <section className="pulse">
            <a href="#welcome">
                <img src={rocket} alt="space"/>
            </a>
        </section>
    )
}
